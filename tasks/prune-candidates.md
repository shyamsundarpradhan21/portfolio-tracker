# Pruning candidates — evidence-based, NOTHING deleted yet

Generated by a reference-graph + grep pass. Execute in batches; sign off per batch.
Verify dead-code with Serena `find_referencing_symbols` before deleting (LSP > grep).

## Batch 1 — Cruft (zero risk, all git-untracked, ~85 MB) ✅ safe
| Path | Size | Tracked? | Note |
|---|---|---|---|
| `_sandbox/` | **79 MB** | untracked | mail-parser spike; CLAUDE.md says "rm -rf _sandbox leaves zero trace" |
| `.playwright-mcp/` | 5.6 MB (191 files) | untracked | browser debug console logs |
| `scripts/capture-in.log` | 233 KB | untracked | runtime log, regenerates |
| `scripts/capture-us.log` | 66 KB | untracked | runtime log, regenerates |
| `scripts/sync.log` | 10 KB | untracked | runtime log, regenerates |
| `fyersRequests.log` | 2 KB | untracked | broker SDK log, regenerates |

Deleting these is invisible to git (0 tracked). Pure disk reclaim.

## Batch 2 — Dead code (low risk; 7 modules, 0 inbound references)
Confirm each with Serena before removing.
- `app/components/shared/InsightBanner.js`
- `app/components/shared/MarketOverview.js`
- `app/components/shared/NiftyOverview.js`
- `app/components/shared/PreMarketBriefing.js`
- `app/components/shared/SectorHeatmap.js`  (only named in MarketOverview's comment — itself dead)
- `app/components/shared/TreeMap.js`
- `app/lib/scenarios.js`  (the `scenarios` grep hits were `PROJECTION.scenarios`, a different object)

These look like a removed "market overview" dashboard section. All `git rm` (tracked source).
NOT dead (false positive): `.claude/statusline.js` — invoked by settings.json, not imported. KEEP.

## Batch 3 — Unused dependency
- `echarts` (^5.5.0) — **zero source mentions** in app/ or scripts/. Heavy lib; removing shrinks install. `npm remove echarts`.
- KEEP `react-dom` — flagged by the scan but required implicitly by Next/React.

## Batch 4 — Duplicate logic (optional refactor, HIGHER risk — do NOT just delete)
- `app/lib/cmpf.js` (`'use client'`, reads hydration) vs `scripts/lib/cmpf.mjs` (pure, reads data).
  Deliberate client/pipeline split, formula locked by `cmpf.test.js`. Consolidating means
  extracting a shared pure core both import — a real refactor, not a deletion. Low payoff.

## Do NOT prune
- `mcp/fyers/` + `login.py` — Fyers auth is **PARKED** (Cloudflare-blocked, feedback.md:314),
  intentionally kept for revival. Parked ≠ dead.
